import fs from 'fs'; let s = fs.readFileSync('server/prisma/schema.prisma', 'utf8'); s = s.replace('  createdAt     DateTime    @default(now())\r\n\r\n  @@map(\"matches\")\r\n}', '  createdAt     DateTime    @default(now())\r\n  playerReviews PlayerReview[]\r\n\r\n  @@map(\"matches\")\r\n}'); s = s.replace('  createdAt     DateTime    @default(now())\n\n  @@map(\"matches\")\n}', '  createdAt     DateTime    @default(now())\n  playerReviews PlayerReview[]\n\n  @@map(\"matches\")\n}'); fs.writeFileSync('server/prisma/schema.prisma', s, 'utf8');
